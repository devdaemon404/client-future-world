import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: { main: '#0d3c61', light: '#fff' },
    secondary: { main: '#2196F3' },
  },
});

export default theme;
